import 'package:flutter/material.dart';

class LiskePage extends StatelessWidget {
  const LiskePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
